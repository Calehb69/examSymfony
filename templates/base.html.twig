<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
        <link rel="stylesheet" href="https://bootswatch.com/5/simplex/bootstrap.min.css"
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
    <body>
        {% block body %}
            <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <a href={{ path('film')}} class="navbar-brand"><strong>BiblioFilm</strong></a>
            <div class="container-fluid">
                <ul>
{#                    <li class="nav-item"><a href="{{ path('signin') }}" class="btn btn-success">Signin</a></li>#}
                </ul>
            </div>
            <li>
{#                {%  if app.user %}#}
{#                <h3>Hello {{ app.user.username }}</h3>#}
{#                <br>#}
{#            <li class="nav-item"><a href="{{ path('signout') }}" class="btn btn-danger">Signout</a></li>#}
{#            {% endif %}#}
            </nav>
        {% endblock %}
    </body>
</html>
